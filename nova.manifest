# nova.manifest — app metadata for the nova updater
NAME=syncman
DESCRIPTION=Bidirectional pair-wise folder sync (game saves etc.) with systemd user services
VERSION=0.1.2
TYPE=gui
SCOPE=user
INSTALLER=install.sh
